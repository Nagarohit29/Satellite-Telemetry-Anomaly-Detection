model: TranAD
dataset: SMAP

training:
  epochs: 5
  batch_size: 128
  learning_rate: 0.0001
  window_size: 10

inference:
  threshold: 0.5
  window_size: 10

paths:
  data_dir: ./data
  model_dir: ./models
  results_dir: ./results

server:
  host: 0.0.0.0
  port: 8001